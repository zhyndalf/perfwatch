# Database
POSTGRES_USER=perfwatch
POSTGRES_PASSWORD=perfwatch
POSTGRES_DB=perfwatch

# Backend - SECURITY: These MUST be changed for production!
# JWT_SECRET must be at least 32 characters. Generate with: openssl rand -base64 32
JWT_SECRET=your-secure-secret-key-at-least-32-chars
JWT_EXPIRE_HOURS=24

# Admin user (for initial setup)
# ADMIN_PASSWORD must be at least 12 characters
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-admin-password
